# PostgreSQL
POSTGRES_USER=training_user
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=training_db
POSTGRES_PORT=5432

# API
PORT=3000
NODE_ENV=development

# Database URL (used by ORM in Phase 1)
DATABASE_URL=postgresql://training_user:changeme_in_production@localhost:5432/training_db
